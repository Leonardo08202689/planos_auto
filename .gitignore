# ── Credenciales (NUNCA subir a Git) ──
.env

# ── Proyectos reales (personales de cada quien; solo las plantillas se comparten) ──
config/proyectos/*.json
!config/proyectos/plantilla.json
!config/proyectos/Plantilla_LAI.json
!config/proyectos/Plantilla_LAI_Municipal.json

# ── Salida generada ──
salida/
*.png
*.pdf
log_*.txt

# ── Python ──
__pycache__/
*.py[cod]
*.pyo

# ── Documentación de terceros descargada (referencia local) ──
docs/pyqgis_cookbook/

# ── Sistema ──
.DS_Store
Thumbs.db
cache_rutas/
cache/
